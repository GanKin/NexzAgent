---
wave: 3
depends_on: ["03-01"]
files_modified:
  - QuantDinger-Vue/src/views/market-overview/SymbolDetail.vue
requirements_addressed:
  - DTL-01
  - DTL-02
  - DTL-03
  - DTL-04
autonomous: true
---

# Plan 03-03: 标的详情时序视图

## Objective

创建标的详情组件（SymbolDetail），在抽屉中展示标的时序数据表格：基础信息卡片 + 日期范围快捷选择 + 按日期降序排列的时序表格，趋势字段颜色标注，变化字段高亮。

## Context

- 抽屉已在 SymbolList.vue 中创建，本组件渲染在抽屉内部
- 时序 API：GET /api/market-overview/symbols/:code/timeline?start_date=&end_date=
- 不使用 ECharts，纯表格展示

## Tasks

### Task 1: 创建 SymbolDetail 组件

<read_first>
- QuantDinger-Vue/src/views/market-overview/SymbolList.vue — 抽屉宿主
- QuantDinger-Vue/src/api/marketOverview.js — API 层
- .planning/phases/03-screening-detail/3-CONTEXT.md — D-13 到 D-20
</read_first>

<action>
创建 `QuantDinger-Vue/src/views/market-overview/SymbolDetail.vue`：

```vue
<template>
  <div class="symbol-detail">
    <a-spin :spinning="loading">
      <template v-if="data">
        <!-- 基础信息卡片 -->
        <a-card size="small" style="margin-bottom: 16px">
          <a-descriptions :column="2" size="small">
            <a-descriptions-item label="代码">{{ data.symbol }}</a-descriptions-item>
            <a-descriptions-item label="名称">{{ data.name }}</a-descriptions-item>
            <a-descriptions-item label="数据范围">{{ data.date_range.start }} ~ {{ data.date_range.end }}</a-descriptions-item>
            <a-descriptions-item label="数据天数">{{ data.timeline.length }} 天</a-descriptions-item>
          </a-descriptions>
        </a-card>

        <!-- 日期范围选择 -->
        <div class="date-range-bar">
          <a-radio-group v-model="dateRange" size="small" @change="onDateRangeChange">
            <a-radio-button value="7">近7天</a-radio-button>
            <a-radio-button value="30">近30天</a-radio-button>
            <a-radio-button value="all">全部</a-radio-button>
          </a-radio-group>
          <a-range-picker
            v-model="customRange"
            size="small"
            style="margin-left: 8px"
            @change="onCustomRangeChange"
          />
        </div>

        <!-- 时序表格 -->
        <a-table
          :columns="timelineColumns"
          :data-source="data.timeline"
          :pagination="false"
          size="small"
          row-key="data_date"
          :scroll="{ y: 400 }"
        >
          <template slot="trend" slot-scope="text">
            <span :class="trendClass(text)">{{ text }}</span>
          </template>
          <template slot="rp_status" slot-scope="text">
            <a-tag :color="rpColor(text)">{{ text }}</a-tag>
          </template>
          <template slot="lev_status" slot-scope="text">
            <span :style="{ color: text === '加杠杆' ? '#52c41a' : '#ff4d4f' }">{{ text }}</span>
          </template>
        </a-table>
      </template>
    </a-spin>
  </div>
</template>
```

props: `symbol` (String)

方法：
- `loadTimeline()` — 调用 getSymbolTimeline(symbol, { start_date, end_date })
- `onDateRangeChange(e)` — 根据快捷按钮计算日期范围，重新加载
- `onCustomRangeChange(dates)` — 自定义日期范围，重新加载
- `trendClass(trend)` — 颜色类名
- `rpColor(status)` — lead→green, Improving→blue, Weakening→orange, Lag→red

时序列定义：
```javascript
timelineColumns: [
  { title: '日期', dataIndex: 'data_date', width: 100, fixed: 'left' },
  { title: '日趋势', dataIndex: 'd_trend', width: 80, scopedSlots: { customRender: 'trend' } },
  { title: '周趋势', dataIndex: 'w_trend', width: 80, scopedSlots: { customRender: 'trend' } },
  { title: '月趋势', dataIndex: 'm_trend', width: 80, scopedSlots: { customRender: 'trend' } },
  { title: '比价', dataIndex: 'relative_price_status', width: 90, scopedSlots: { customRender: 'rp_status' } },
  { title: '比价天数', dataIndex: 'relative_price_duration', width: 70 },
  { title: '杠杆', dataIndex: 'leverage_status', width: 70, scopedSlots: { customRender: 'lev_status' } },
  { title: '杠杆值', dataIndex: 'leverage_value', width: 70, customRender: v => v != null ? Number(v).toFixed(1) : '-' },
  { title: '杠杆变动', dataIndex: 'leverage_change', width: 70, customRender: v => v != null ? Number(v).toFixed(1) : '-' },
  { title: '相对强度', dataIndex: 'relative_strength', width: 70 },
  { title: '60日位', dataIndex: 'price_position_60d', width: 60, customRender: v => v != null ? Number(v).toFixed(2) : '-' },
]
```

生命周期：`created()` → `loadTimeline()`

默认日期范围：近30天（从今天算起）
</action>

<verify>
  <automated>test -f QuantDinger-Vue/src/views/market-overview/SymbolDetail.vue && echo "EXISTS"</automated>
</verify>

<acceptance_criteria>
- `SymbolDetail.vue` 存在
- 包含 `<a-descriptions>` 基础信息展示
- 包含 `<a-radio-group>` 快捷日期按钮（7天/30天/全部）
- 包含 `<a-range-picker>` 自定义日期范围
- 包含 `<a-table>` 时序表格
- 时序列包含日期、D/W/M趋势、比价、杠杆、杠杆值、相对强度、60日位
- 趋势字段用颜色标注
- 比价字段用 a-tag 标签
</acceptance_criteria>

<done>
标的详情时序组件创建完成。快捷日期按钮 + 时序表格 + 颜色标注。
</done>

## must_haves

- 基础信息（代码、名称、数据范围）
- 快捷日期按钮（7d/30d/全部）+ 日历
- 时序表格按日期降序，趋势颜色标注
- 比价状态标签颜色区分
- 杠杆状态颜色区分

## success_criteria

1. 打开详情抽屉显示标的时序数据
2. 切换日期范围正确筛选
3. 趋势字段上行=绿、下行=红、静默=黄
4. 无数据时显示空状态
5. 表格可纵向滚动
